<h1>Trainer</h1>


<%= link_to "<< Back to List", trainers_path%>

<div>
  <table>
    <tr>
      <th>Name</th>
      <td><%= @trainer.name %></td>
    </tr>
    <tr>
      <th>Gender</th>
      <td><%= @trainer.gender %></td>
    </tr>
    <tr>
      <th>Created</th>
      <td><%= @trainer.created_at %></td>
    </tr>
    <tr>
      <th>Updated</th>
      <td><%= @trainer.updated_at %></td>
    </tr>
  </table>
<br>

<h3>Feedbacks</h3>
  <%=link_to "Add Feedback for this Trainer", new_feedback_path(@trainer) %><br>

  <%@trainer.feedbacks.each do |fd|%>
    <%= fd.rating%> star -  <%= fd.comment%> 
     <%= link_to "edit", edit_feedback_path(fd) %> 
     <%# <%= link_to "Delet", delete_feedback_path(fd) %> 
     <%= link_to "Delet", delete_feedback_path(fd)%><br>   
  <%end%>
  

<h3>Appointments List</h3>
  <table>
    <tr>
      <th>Client Name</th>
      <th>Appointment time</th>
    </tr>
  <% @trainer.appointments.each do |appt|%>
    <tr>
      <td><%=appt.client.name%></td>
      <td><%= appt.time%></td>
    </tr>
      <%end%>
 </table>
</div>

