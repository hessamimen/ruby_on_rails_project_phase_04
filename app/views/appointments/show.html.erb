<h3>Appointment Details</h3>

<%= link_to "<< Back to List", appointments_path%>
<br>
<br>


  <table>
    <tr>
      <th>Client's Name</th>
      <th>Trainer's Name</th>
      <th>Appointment Time</th>
      <th> Action </th>
    </tr>
    <tr>
    <td><%= @client.name %></td> 
    <td><%= @trainer.name %></td> 
    <td><%= @appointment.time.strftime("%b %d, %Y at %k:%M")%></td> 
    <td><%= link_to "Edit", edit_appointment_path(@appointment) %></td>
    <td><%= link_to "Delete", delete_appointment_path(@appointment) %></td>
    
    </tr>
 <table>